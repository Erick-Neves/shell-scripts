name: Deploy to Production

on:
  push:
    branches:
      - main  # ajuste conforme necessário

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Create SSH
      run: |
        ssh-copy-id prod_user@191.96.251.3
        ssh prod_user@191.96.251.3
        docker ps
